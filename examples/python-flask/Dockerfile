# Dockerfile for python-flask example
# Add appropriate base image and build steps for python-flask

FROM alpine:latest
RUN echo "Add build steps for python-flask here"
EXPOSE 8080
CMD ["echo", "Start command for python-flask"]
