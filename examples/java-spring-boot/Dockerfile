# Dockerfile for java-spring-boot example
# Add appropriate base image and build steps for java-spring-boot

FROM alpine:latest
RUN echo "Add build steps for java-spring-boot here"
EXPOSE 8080
CMD ["echo", "Start command for java-spring-boot"]
