# node-express CI/CD Pipeline Example
stages:
  - build
  - test
  - quality
  - deploy

variables:
  DOCKER_REGISTRY: "localhost:5050"

build_node-express:
  stage: build
  script:
    - echo "Building node-express application"
    # Add build commands specific to node-express

test_node-express:
  stage: test
  script:
    - echo "Testing node-express application"
    # Add test commands specific to node-express

quality_check:
  stage: quality
  image: sonarsource/sonar-scanner-cli:latest
  script:
    - sonar-scanner
  only:
    - main
    - develop

deploy_node-express:
  stage: deploy
  script:
    - echo "Deploying node-express application"
    # Add deployment commands
  only:
    - main
