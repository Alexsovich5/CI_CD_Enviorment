# Dockerfile for node-express example
# Add appropriate base image and build steps for node-express

FROM alpine:latest
RUN echo "Add build steps for node-express here"
EXPOSE 8080
CMD ["echo", "Start command for node-express"]
